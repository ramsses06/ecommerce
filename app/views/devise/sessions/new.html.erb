<div class="col-md-6 col-sm-12 col-xs-12 no-float center-block">
  <div class="card medium-padding">

    <h2 class="text-center">Inicio de Sesión</h2>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), builder: BootstrapFormBuilder) do |f| %>
      <div class="form-group">
        <%= f.email_field :email, autofocus: true, placeholder:"Correo electrónico" %>
      </div>

      <div class="form-group">
        <%= f.password_field :password, autocomplete: "off", placeholder: "Contraseña" %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div class="checkbox-material">
          <label>
            <%= f.check_box :remember_me %> Recordar
          </label>
        </div>
      <% end -%>

      <div class="actions text-center">
        <%= f.submit "Iniciar Sesión" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>

  </div>
</div>
